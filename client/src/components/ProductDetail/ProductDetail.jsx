import React from "react";

import S1 from "./LayoutDesign/S1";

const ProductDetail = ({ productInfo, owner }) => {
  return <S1 productInfo={productInfo} owner={owner} />;
};

export default ProductDetail;
